//double(n)
I predict that the entered number will be doubled as the output.

// square(n) 
I predict that the entered number will be squared as the output. 

// isEven(n)
I predict that if a number is divisible by 2 it will return true. If not, it will return false.

// isOdd(n) 
I predict that if a number that isnt divible by 2 is chosen, it will return true. If not, it will return false.

// multiply(a, b) 
I predict that multiply(2, 4) will return 8 since the funtion multiplies the two numbers. 
